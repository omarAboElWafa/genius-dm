'use strict';

/**
 * setting-contact controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::setting-contact.setting-contact');
